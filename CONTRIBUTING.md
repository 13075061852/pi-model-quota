# Contributing

感谢帮助改进 Pi Model Quota！小型修复可直接提交 PR；新增平台或改变兑换行为，请先开 Issue 讨论。

## 本地开发

- 使用 Node.js 22.18+（推荐当前 LTS）和兼容版本的 Pi。
- 克隆仓库后运行 `npm test`；测试只依赖 Node 内置模块，无需真实账户。
- 保持 TypeScript ESM 风格、显式 `.ts` 导入及现有缩进。
- 解析逻辑放在 `quota-core.ts`，网络放在 `probes.ts`，UI/生命周期放在 `index.ts`。

## 提交前检查

```bash
npm test
git diff --check
```

- 为新平台、字段缺失和失败分支补充测试。
- 修改命令或展示行为时更新 README、Skill 和 CHANGELOG。
- 不提交 token、认证文件、未经脱敏的网络响应或个人账户截图。
- 网络测试必须 mock；不得消耗真实重置卡，不得进行真实购买。
- 重置操作必须保留用户确认、来源校验、防重复提交及不确定结果提示。
- 保持单一职责；提交信息说明改动目的，PR 描述列出测试结果与限制。

## 兼容性

上游内部接口可能变化。提供字段样例时，请使用合成数据；不要从账户抓包中直接复制凭据。声明支持新 Node/Pi 版本前请验证，不要仅放宽版本范围。
