# Security Policy

## 支持范围

安全修复优先针对默认分支和最新发布版本；旧版本不承诺回补。

## 报告漏洞

不要在公开 Issue 中上传 token、`auth.json`、原始响应头或账户身份信息。

如仓库已启用 GitHub Private vulnerability reporting，请在 Security → Advisories 中私密报告。若未启用，请先开一个不含漏洞细节的 Issue，请维护者提供私密沟通渠道；不要公开利用步骤或凭据。

报告中说明受影响版本、影响范围和使用合成数据的复现方式。

## 安全边界

- 本扩展与其他 Pi 扩展一样在本机进程内运行，不是隔离沙箱。
- 自动查询是只读操作。`/quota reset` 确认后是会消耗已有卡的账户写操作，不是购买。
- 不确定的 POST 结果不会自动重试；当前实例会阻止再次兑换。该锁不跨进程、重启或重载持久化。
- 所有自动测试必须使用模拟网络，禁止使用真实重置卡。
- 上游 Codex 接口不是稳定公共 API，兼容性可能变化。
